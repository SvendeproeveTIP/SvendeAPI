{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Saving password in here because we dont have an azure vault, and we cant save it on the machine via user secrets.
  // Becasue we are going to switch from macOS to a linux server when the API is finished.
  "ConnectionStrings": {
    "LeMoun": "Server=5.9.142.245; Port=3306; Database=transportudlejning; Uid= transportudlejning; Password=alhamdulillah;"
  }
}
